# 验收报告：metanet4j 升级到 Spring Boot 4.1.1 + Java 25（版本号 0.2.0）

> 本文件是 P6 交付物：**受影响仓库、每仓库编译命令、每仓库提交 ID**，以及可复现的门禁结果。
> 目标、版本矩阵、决策 D1–D26、改动清单与逐轮执行记录见 `升级计划-Boot4-Java25.md`，本文件不复述。
> 连接信息唯一事实来源：`ownword/infra/README-*.md`；Maven 命令唯一事实来源：`../mvn-command.md`。

## 1. 验收结论：通过

| 维度 | 结论 |
|---|---|
| 编译门禁（4 仓库，含 test-compile） | ✅ 全部 BUILD SUCCESS |
| 测试执行数（按模块，非总数口径） | ✅ base 2 / sdk 23 / connect-planaria 1 / component-file 8 / component-test 95，均 >0 且 **0 error 0 failure** |
| 上下文冒烟 `contextLoads` | ✅ 在 8 个类的 `TEST-*.xml` 中真实执行 PASS |
| 生产依赖全局 grep 门禁（§6.6 九条） | ✅ 全部通过 |
| 依赖漂移断言（§7.2） | ✅ 全部符合 |
| B 档实连（共享中间件） | ✅ Mongo 8.0.32 / ES 9.4.5 / Kafka 4.2.1 KRaft / Redis 7.4.11 / MySQL 8.4.11 相关用例真实执行 |
| 环境隔离（全局工具链未污染） | ✅ 全局 JDK `1.8.0_181` / Maven `3.9.9` 保持原样（`./init.sh` 每次校验） |

升级后的关键坐标：Spring Boot **4.1.1**、Spring Cloud **2025.1.3**、Java **25**、`elasticsearch-java` **9.4.5**、
`kafka-clients` **4.2.1**、Redisson **4.7.0**（`redisson-spring-data-41`）、MyBatis-Plus **3.5.17**、Druid **1.2.28**、
`mongodb-driver-sync` **5.8.1**、`mysql-connector-j` **9.7.0**、Jackson **3.1.5**、lombok **1.18.46**。

## 2. 受影响仓库与提交 ID

四仓库均为 worktree，分支 **`feature/java21`**，基线 **`dev`**（开工时与 `feature/java21` 同点）；
四仓库均**只领先 dev、未落后**、工作区 0 脏。
**已推送为远端 `feature/java21`**（2026-09-16 两轮：先推交付成果，再推"交付后修复"；远端 `dev`/`master` 未被触碰，
推送后逐仓库核对本地 HEAD 与 `origin/feature/java21` 完全一致）。

| # | 仓库 | 分支 | 基线 | 本次提交数 | HEAD（最终提交 ID） |
|---|---|---|---|---|---|
| 1 | `metanet4j-parent` | feature/java21 | dev | 4 | **`50598c0`** |
| 2 | `metanet4j-base` | feature/java21 | dev | 3 | **`6e16cfa`** |
| 3 | `metanet4j-sdk` | feature/java21 | dev | 5 | **`bae4c36`** |
| 4 | `metanet4j-component` | feature/java21 | dev | 10 | **`d585194`** |

### 2.1 逐仓库提交清单（`dev..HEAD`，由旧到新）

**metanet4j-parent**（4）
```
5f462fa chore(pom): 版本号统一 0.2.0 并移除 java.version=11 覆盖
935b5f5 build(pom): 升级父 POM 到 Boot 4.1.1 / Java 25 并清理钉死
a82ab4e fix(pom): 删除两条不带 version 的 depMgmt 条目（会屏蔽 Boot BOM）
50598c0 build(pom): 测试统一 JUnit 5，移除 JUnit4 属性与 depMgmt 条目
```

**metanet4j-base**（3）
```
d0e2384 chore(pom): 版本号统一 0.2.0
4f5a65a feat(base): 迁移到 Jakarta Validation 与 Jackson 3
6e16cfa test(base): 测试迁移到 JUnit 5（Jupiter）
```

**metanet4j-sdk**（4）
```
52e59bb chore(pom): 版本号统一 0.2.0
097870d feat(sdk): 注解迁移到 JSpecify / Jakarta 并显式引入 jspecify
451020e fix(sdk): 修复 BapBase 构造链缺陷并把测试迁移到 Jupiter
bc966e5 test(sdk): 联网交易用例打 @Tag("external") 并从常规门禁排除
bae4c36 fix(sdk): BAP root/id 交易的签名类型不再失效（root 必须用根地址签名）   ← 交付后修复
```

**metanet4j-component**（9）
```
d677634 build: 移除 5 个模块中过期的 Maven Wrapper（锁定 3.8.2/3.8.3）
6ecd226 chore(pom): 22 个 pom 版本号统一 0.2.0 并移除 12 处 java.version=11 覆盖
b468ba8 feat(component): 迁移到 Boot 4.1.1 / Jakarta / Jackson 3 / ES 9.4.5 / Redisson 4.7.0
0ce5c18 test(component): 测试统一迁移到 JUnit 5（撤掉 vintage 方案）
5deced9 fix(component): P5 联调修复 Boot 4 配置前缀与 Feign 依赖
8d21caf fix(component-test): P5 收敛——planaria 配置绑定、ES 索引幂等、外部依赖打 Tag
8df13c6 test(component-test): 再补两个 external Tag（store-sql 依赖 / 本机绝对路径数据）
58dc786 docs(component-test): 记录 Mongo 两类认证问题的诊断结论（实验无效已回滚）
73e3ad4 fix(component-test): P5 收敛 component-test 全绿（YAML 顶层键破坏 spring.* 绑定 + 夹具缺陷）
d585194 fix(component-test): 对齐共享设施 Redis/MySQL 凭据，并撤掉 sdk 缺陷的测试绕过   ← 交付后修复
```

**ownword**（任务文档与共享中间件，非代码仓库）
```
8770634 chore(infra): 共享中间件 ownword/infra
15406eb chore(boot4): 任务 harness
4a28fe0 docs(boot4): 任务文档纳入版本控制
9084bd0 / 4f76872 / adf8180 / 42e255a / 193d74a / ac77845 / 8e21d7a  docs(boot4): 逐轮执行记录
3d1f143 docs(boot4-java25-upgrade): P5 收口
8c88193 docs(boot4-java25-upgrade): P6 收尾——输出验收报告并同步全部文档（本文件即该提交的交付物）
0e59ede docs(boot4-java25-upgrade): 验收报告补 ownword 侧提交 ID（8c88193），去掉交付物里的占位符
（交付后修复轮的文档提交见 git log：docs(boot4-java25-upgrade): 记录两处交付后修复与两轮推送）
```

## 3. 每仓库编译命令

**固定前置**（每次都要；不设 `JAVA_HOME` 会落到全局 JDK 8，`--release 25` 直接失败；`-s` 保证依赖进隔离仓库）：

```bash
TASK=/home/haodev/ownword/_task/backend-worktree/boot4-java25-upgrade
export JAVA_HOME=$HOME/.sdkman/candidates/java/25.0.4.1-tem
MVN="$HOME/.sdkman/candidates/maven/3.9.16/bin/mvn -s $HOME/.m2/metanet4j-settings.xml -B"
```

**逐仓库**（顺序不可乱：`parent` 先 install，下游才能解析父 POM；子模块 `<parent>` 未写 `relativePath`）：

```bash
# ① parent（packaging=pom，只发 POM，必须 -N）
(cd $TASK/metanet4j-parent && $MVN -N install)

# ② base
(cd $TASK/metanet4j-base && $MVN clean install)

# ③ sdk
(cd $TASK/metanet4j-sdk && $MVN clean install)

# ④ component（聚合工程，递归 21 个子模块）
(cd $TASK/metanet4j-component && $MVN clean package)
```

**只做编译门禁**（跳过用例执行，仍编译测试代码）：在上面第 ②③④ 条末尾加 `-DskipTests`。

**跑测试门禁**（排除需联网/外部依赖的用例；这是本报告 §4.2 的口径）：

```bash
(cd $TASK/metanet4j-base      && $MVN clean test)
(cd $TASK/metanet4j-sdk       && $MVN clean test -DexcludedGroups=external)
(cd $TASK/metanet4j-component && $MVN clean test -DexcludedGroups=external)
```

**一键环境与状态自检**：`cd $TASK && ./init.sh`（秒级；`--full` 追加真实构建与分模块执行数）。

## 4. 门禁结果（实测）

### 4.1 测试执行数（`-DexcludedGroups=external`）

| 模块 | 执行数 | 失败 | 错误 | 跳过 | 结论 |
|---|---|---|---|---|---|
| metanet4j-base | 2 | 0 | 0 | 0 | ✅ |
| metanet4j-sdk | 26 | 0 | 0 | 0 | ✅（含交付后新增 3 个离线回归用例；另 29 个 external 被排除） |
| metanet4j-connect-planaria | 1 | 0 | 0 | 0 | ✅ |
| metanet4j-component-file | 8 | 0 | 0 | 8 | ✅（skipped 为需 S3/SFTP 凭据的用例） |
| metanet4j-component-test | 95 | 0 | 0 | 0 | ✅ |

### 4.2 用例账目（component-test 静态 129 个用例实例的去向，逐项可解释）

| 去向 | 数量 | 说明 |
|---|---|---|
| 常规门禁执行 | 95 | 上表实测 |
| `@Tag("external")` 排除 | 30 | 类级 27（`ComplteTxFactoryTest` 4、`BsocailConvertorTest` 11、`BlockTaskServiceTest` 5、`BapConvertorTest` 4、`TxUtxoServiceTest` 2、`MetaIdConvertorTest` 1）+ 方法级 3（`BapRawStrResolverTest#testHandleBapId`、`BsocialRawResolverTest` 2 个） |
| 从未被 surefire 选中 | 3 | `convertor/TxoBobConverter.java` 的类名不含 `Test`，不匹配 surefire 默认 includes（上游遗留） |
| Jupiter 静默忽略 | 1 | 夹具类 `BitcoinschemaTransactionTest#testBsocialReply` 是 `@Test public String ...`（非 void） |

> sdk 用例账目：静态 55 = 26 常规执行 + 29 `@Tag("external")` 排除（13+13+3 三个公网广播类）。

> `contextLoads` 判定口径：surefire 的 **`.txt` 报告只记录失败项**，通过用例不会出现 →
> 必须看 `TEST-*.xml`（本次 `contextLoads` 在 8 个类的 XML 中 PASS）。用 `grep *.txt` 判会误报"未执行"。

### 4.3 §6.6 九条全局 grep 门禁

| # | 门禁 | 实测 | 期望 |
|---|---|---|---|
| 1 | pom 总数 | 25 | 25 |
| 2 | `<version>0.1.0</version>` | 0 | 0 |
| 3 | `<metanet4j.version>0.1.0</metanet4j.version>` | 0 | 0 |
| 4 | `<java.version>11</java.version>` 文件数 | 0 | 0 |
| 5 | `import javax.*` | 仅 `AesCBCUtil`（`javax.crypto.*`，JDK 自带） | 白名单外 0 |
| 6 | Boot 4 旧包名（`autoconfigure.jdbc/orm.jpa/http.HttpMessageConverters/data.redis`） | 0 | 0 |
| 7 | Jackson 旧 `com.fasterxml.jackson.{core,databind,datatype,module}` | 0 | 0 |
| 8 | `RedissonAutoConfigurationV2` | 0 | 0 |
| 9 | `org.apache.http.HttpHost` / ES `RestClient` / `RestClientTransport` | 0 | 0 |

### 4.4 依赖漂移断言（`dependency:tree`）

| 断言 | 实测 |
|---|---|
| ES 客户端 | `co.elastic.clients:elasticsearch-java:9.4.5` + `elasticsearch-rest5-client:9.4.5`；**无** legacy `org.elasticsearch.client:elasticsearch-rest-client` |
| Redisson | `org.redisson:redisson-spring-data-41:4.7.0`；**无** `redisson-spring-data-2x` |
| Kafka | `org.apache.kafka:kafka-clients:4.2.1` |
| Mongo | `org.mongodb:mongodb-driver-sync:5.8.1` |
| MySQL | `com.mysql:mysql-connector-j:9.7.0` |

### 4.5 B 档（共享中间件实连）

`ownword/infra`（compose 项目 `ownword-infra`）五容器 healthy：MongoDB 8.0.32、Elasticsearch 9.4.5、
Kafka 4.2.1（KRaft）、Redis 7.4.11、MySQL 8.4.11。相关用例真实执行（非 mock）：Mongo 写入/查询、ES 建索引与检索、
Kafka 生产消费、Redis/事务系列。

## 5. 未决项与遗留（需用户决策）

| # | 事项 | 影响 | 建议 |
|---|---|---|---|
| 1 | ~~**sdk `initSignType` 是死代码**~~ | — | ✅ **已修**（用户 2026-09-16 指示"先推当前成果、修复后再推一次"）：`initSignType` 改为直接写入 `signType`；sdk 提交 **`bae4c36`**。证据：新增离线回归用例 `BapDataLockBuilderSignTypeTest`（3 用例断言 AIP 签名地址 = root/previous/current）+ **负向验证**（回退修复后 2 个用例失败，报错正是 expected root `1QBoVK…` but was current `1PpNsZ…`）+ sdk 26/0/0 + component-test 夹具撤掉 3 参构造器绕过恢复 `buildXxx().sign()` 后 resolver 两类全绿 |
| 2 | ~~**测试应用配置与共享设施不一致**~~ | — | ✅ **已对齐**：Redis 去掉 password、MySQL 改 `root/root123` 并补 `allowPublicKeyRetrieval=true`；component 提交 **`d585194`**。证据：`docker exec infra-mysql` 旧密码 `Access denied`、新密码可连 8.4.11；Redis `CONFIG GET requirepass` 为空且无密码 PING=PONG；Environment 实测绑定生效。注：该应用类路径无 `spring-data-redis`、无 `DataSource` Bean，故这两组配置在此应用中不建立运行时连接（属配置卫生） |
| 3 | 未打基线 tag `pre-boot4-java25` | 计划 §8 曾要求"开工前打 tag"。实测四仓库 `dev` 开工后**未移动**（领先 N、落后 0），回滚等价于 `git checkout dev` | 如需留痕可补打 tag；否则以本报告记录替代 |
| 4 | 58 个用例被 `@Tag("external")` 排除（sdk 29 + component 26 + …） | 需公网（Bitails/GorillaPool 实时 UTXO 与主网广播）、store-sql/MySQL、S3/SFTP 凭据、或引用 2023 年主网 outpoint | 已逐类定性归档；不进常规门禁 |
| 5 | `TxoBobConverter`（3 个用例）从未被 surefire 选中 | 类名不含 `Test`，不匹配 surefire 默认 includes（上游遗留） | 如需恢复执行，重命名为 `TxoBobConverterTest` 即可（本次未改，避免越界） |

## 6. 声明

- **远端状态**：四个代码仓库已推送 `origin/feature/java21`（两轮：交付成果 → 交付后修复），推送后核对
  本地 HEAD 与远端引用一致；**`dev`/`master` 未被触碰**。ownword（任务文档）仍为本地提交，未推送。
- 分支为 `feature/java21`（计划 §8 原写的 `feat/boot4-java25-*` 未采用；实际沿用任务既有分支，`./init.sh` 也按此校验）。
- 本次升级**未改动全局** JDK/Maven 默认版本、未改 `~/.bashrc` 等；工具链全部用绝对路径调用。
- 共享中间件为跨任务设施，**不要 `down -v`**（会清掉跨任务数据）。
