# 当前进展

当前事项为 `design-astra-001`，状态以 `feature_list.json` 为准。原型作为产品文档，保留现有 React/Babel、模拟数据与 S2，不做生产构建迁移或真实服务接入。

## 当前成果

- 编辑页：收紧当前身份信息，标识使用辅助字号；头像与字段分组，资料字段增加留白，短屏字段间距为 24px。仅改 CSS，创建与确认页样式、文案、业务流程不变。
- 页面标题：平衡断行，中文行距 1.3、无负字距，窄屏使用较小一级 S2 字号。
- 公开身份卡：背面移至厚度层外侧，两面共享自适应高度，背面内容可见且不溢出；保留轻薄侧边与阴影、长资料摘要和完整内容折叠、链上记录与复制入口。
- 页头滚动吸顶；320px 已连接页头高 120px。全局提示位于底部，6 秒自动消失，支持暂停与手动关闭。
- 原型场景与示例说明集中于右下角设置；授权弹窗明确外部钱包边界。

## 本次验证

按 [原型验证范围](AGENTS.md#原型验证范围)，仅检查卡片及发现的关联高度问题：

- 1440×1000 英文浅色、320×568 中文深色：实际翻面并等待过渡结束，背面名称、区块高度、确认状态、交易 ID 与 BSV 可见；两张截图已复核。
- 背面内容高度与容器高度一致，不覆盖下方复制区；复制按钮反馈成功，窄屏按钮高 44px。浏览器拒绝读取剪贴板，未核对系统剪贴板值。
- 1440×1000 中文深色实际拖回正面，临时截图复核通过。无横向溢出，页面运行错误为空，`git diff --check` 通过。

证据：[card-back-check.json](../../designs/own-word-prototype-s2-astra-001/evidence/card-back-check.json) 及同目录 `card-back-desktop.png`、`card-back-mobile.png`。

未跑全量或整套专项回归，未穷举语言、主题、屏宽与长内容组合。既有证据保留在原型 `evidence/`，不代表本次重跑；卡片是否可见必须查看实际截图，不能仅凭翻面状态或 DOM 文本判断。原型无真实钱包、网络交易或数据库连接。

## 待办与下一步

用户预览复核公开身份卡背面，再按反馈调整。资产保持 `needs-review`，整体事项尚未完成；其他生产契约问题不在本次修复范围。

预览：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/
