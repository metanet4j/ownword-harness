# 当前进展

v0.1 基线事项为 `design-astra-001`，保留现有 React/Babel、模拟数据与 S2。v0.2.0 的文字内容规划已加入真实身份签名和链上发布要求；状态以 `feature_list.json` 为准。

## 当前成果

- 我的身份页：小字号页面标题，发布状态靠近姓名；BAP ID、操作与简介按单列组织，长简介完整换行。实现与视觉以原型为准。

- 编辑页：收紧当前身份信息，标识使用辅助字号；头像与字段分组，资料字段增加留白，短屏字段间距为 24px。实现以对应页面样式为准。
- 手机创建页：步骤、标题和身份摘要更紧凑；头像标签与选图按钮并排，空间不足时自然换行，保留字段间距与 44px 按钮。
- 页面标题：平衡断行，中文行距 1.3、无负字距，窄屏使用较小一级 S2 字号。
- 公开身份卡：收紧正反面间距及卡外复制区，两面共享自适应高度；新增“翻面／翻回正面”按钮，支持键盘，保留拖动；进入页面时轻动一次，2.4 秒后静止。长资料摘要、完整资料及链上记录折叠继续可用。
- 确认页：切页回到顶部并聚焦标题，步骤和标题完整可见；BAP ID 自动换行，复制按钮独立保留。
- 页头滚动吸顶；320px 已连接页头高 120px。全局提示位于底部，6 秒自动消失，支持暂停与手动关闭；普通通知采用紧凑布局。
- 原型场景与示例说明集中于右下角设置；授权弹窗明确外部钱包边界。

## 当前基线与验证证据

第二轮四项优化已实现并提交审阅。实现以原型为准，事实依据为 PRD v0.1 第 5.6 节、核心认知第 6.3 与 10.5 节。

- 字段纠错：首次提交后，错误随当前输入更新。390×844 中文浅色下，空名称必填、填写后清除、名称与简介超限及恢复均通过；正常模拟创建完成。
- 无改动保存：未修改及改回原值时显示“暂无修改”，无法进入保存授权；回车提交守卫通过，真实修改可正常保存。模型验证覆盖名称、简介、类型和头像字段。
- 短屏提示：320×568 中文深色取消创建后，提示由约 164px 缩至 79px，关闭按钮仍高 44px，未覆盖底部操作且无横向溢出；键盘关闭后草稿保留。
- 公开卡动效：轻动 2.4 秒一次，结束后静止；手动拖动和键盘翻面、翻回通过。轻动中翻面立即停止动画，超过原动画时长后仍保持手动角度。320px 减少动态效果模式跳过轻动，仍可翻面。
- 已查看六张修复后截图；浏览器运行错误检查为空，运行日志已保存。92 条模型断言、4 项文案契约、66 个 token 引用、浏览器检查脚本语法与 `git diff --check` 通过。
- 浏览器验证使用宿主 `agent-browser` 定向操作，未执行整份 `check-browser.py`、全量组合、真实手机键盘/触屏或真实钱包验证。既有回归结果不计入本轮通过结果。

证据：[四项修复验证记录](../../designs/own-word-prototype-s2-astra-001/evidence/feedback-motion-20260917/verification.json)、同目录截图与 `console.txt`。实现提交：`4e6b227`、`060fa8a`、`0c00ce2`、`4265f03`。

## 分支交付

用户已要求提交并合并到 `dev`。原型与根仓库原先均无此分支，现已分别从当前提交建立 `dev`，推送目标为 `origin/dev`；原型包含四项实现及证据，提交为 `8d924ae`。分支建立未改变产品文件，无合并冲突，沿用本轮定向验证结果。根仓库原有技能文件改动未纳入提交。

## v0.2.0 规划产出

用户已确认基于当前版本制作文字内容高保真原型，Markdown 参考 xLog，发布必须支持身份签名和真实链上发布。已提交[环境与功能计划](v0.2.0-内容原型实施计划-20260917-204043.md)供评审，未来事项 `design-content-v020` 保持 `not-started`；当前原型及核心认知未修改。

- 建议从原型 `dev@8d924ae` 建立独立 worktree，复用 React/Babel、S2 与 4311 静态服务；增加 CodeMirror、统一 Markdown 渲染器和经验证的钱包适配依赖构建。
- 计划覆盖内容工作台、xLog 式 Markdown 编辑、草稿、导入导出、真实身份签名与链上发布、按 TxID 阅读、Revision 和 Proof。真实钱包、BAP 解析、内容签名和交易读回验证提前至 P0。
- 已阅读本地 xLog 的双栏编辑器、CodeMirror、工具栏、草稿和渲染代码，并核对官方来源。xLog 线上站点未能正常访问，未进行线上编辑实操。
- 文字内容范围已确认，与现有 Markdown Post 模型一致；编辑器参考 xLog。标题、长度及文件大小约束在 P0 形成可评审规则。
- 已核对核心认知、钱包 Provider API 和 BitcoinSchema Post 文档。当前资料尚不能证明具体钱包签名封装可直接完成 Post，P0 必须以实际输出及独立验证器核实。模拟发布不计入验收。
- 本轮只验证计划引用、JSON 状态结构及差异格式；未搭建 v0.2.0 环境，未连接真实钱包或发送交易。

## 下一步

按已确认范围推进 P0：版本 PRD、独立环境、真实钱包与最小签名发布链路验证，再交付 P1 编辑器与草稿。当前 v0.1 整体事项仍保留人工复核状态，不因新版本规划自动标记完成。

当前原型：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/
