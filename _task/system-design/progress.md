# 当前进展

当前事项为 `design-astra-001`，状态以 `feature_list.json` 为准。原型作为产品文档持续打磨，沿用现有 React/Babel、模拟数据与 S2。

## 已完成

- 公开卡下方信息区：两个折叠入口改为左侧标题、右侧 S2 箭头，收紧间距；箭头跟随展开状态，仅改 CSS，保留原有交互与 44px 点击高度。

- 长内容边界：公开身份卡显示昵称两行、简介四行摘要，完整内容通过卡片下方按钮展开；编辑、确认、身份页与原始资料不截断，长度规则不变。复用 S2 按钮与既有折叠样式，避免长卡片投影覆盖复制区。

- 窄屏页头收紧，320px 已连接页头从 168px 减至 120px，未连接时单行；按钮文字与至少 44px 点击高度保留，桌面布局不变。沿用 S2，仅修改响应式 CSS。

- 公开身份卡下方增加链上记录折叠入口，与卡片背面共用展示；支持键盘展开／收起和完整 TxID 复制，保留 3D 效果。

- 顶部导航滚动吸顶；全局提示显示在底部，6 秒自动消失，悬停／键盘焦点暂停，允许手动关闭。错误状态、草稿及重试入口保留，字段校验不变；小屏提示避让固定操作条，页脚原型入口可点击。

- 场景控制集中到右下角原型设置，失败与账户事件可配置，取消、重试及旧结果隔离保持可用。原型提交：`42168a0`。
- 示例数据说明集中到设置；授权弹窗标注外部钱包边界，产品提示仍标注 OWNWORD。原型提交：`f56ce04`。
- 操作说明与证据：[原型演练](../../designs/own-word-prototype-s2-astra-001/原型演练_20260916-1604.md)。

## 验证

验证遵循 [任务指南的原型验证范围](AGENTS.md#原型验证范围)。本次仅检查桌面英文浅色点击展开／收起、320px 中文深色 Enter／Space 操作，并复核桌面和窄屏英文浅色截图。箭头状态正确，按钮高 44px，窄屏无横向溢出，运行错误为空；66 个 token 全部解析。证据见原型 `evidence/info-polish-*`。未跑全量或整套专项回归；以下为既有验证结果。

本轮长内容专项 146 条通过；100 字符昵称、1000 字符简介，覆盖连续英文、长链接、中文、换行、表情及中英×浅深、320/768/1440px。8 次完整资料折叠区 axe 无 violation/incomplete，截图已复核，运行错误为空。模型 74、词典 4（160 个 key）、65 个 token 通过。证据见 `evidence/long-content-*`；未重跑全量 BDD。

同轮链上记录回归 24 条通过，复制、键盘展开及卡片拖动保持正常；4 次链上记录折叠区 axe 无 violation/incomplete。

本轮窄屏页头专项 40 条通过；中英×浅深、320/390/650/768/1440px 布局、吸顶、键盘偏好操作和导航均通过。4 次页头 axe 无 violation/incomplete；截图已复核。证据见原型内 `evidence/narrow-header-*`。模型 74、词典 4、65 个 token 通过。导航与底部提示专项重跑 29 条通过，4 次 axe 无 violation，4 条背景文字 incomplete 保留。

当前链上记录专项 24 条通过，4 次折叠区 axe 审计无 violation/incomplete；中英×浅深、320/1440px、完整剪贴板值、复制失败和已确认／待确认状态均有证据，见原型内 `evidence/chain-disclosure-*`。此前专项为各批次基线，本轮未重跑全量 BDD。

模型 74 条、词典 4 条、65 个 token、专项浏览器 45 条通过。中英×浅深、320×568 设置与授权弹窗可用；8 次 axe 审计 0 violation，4 条背景文字 incomplete 已记录。中文关闭按钮不换行，截图与 DOM 复核通过，无页面运行错误。

未重跑全量 BDD；既有全量证据仅作基线。本原型无真实钱包、网络交易或数据库连接。

当前导航与提示专项 29 条通过，4 次 axe 审计无 violation，4 条背景文字与导航 color-contrast incomplete 已记录并查看截图。证据：原型内 `evidence/header-feedback-results.json`、`header-feedback-errors.txt` 和对应截图。此前场景专项 45 条为前两批基线，本轮未重跑该专项。

## 待办

- 用户预览复核，资产保持 `needs-review`。
- 链上记录的键盘访问缺口已通过卡片下方折叠区补齐。
- 已有交接资料中的其他生产契约问题不在本次场景整理范围。

## 下一步

用户复核公开卡下方信息区的对齐、箭头与间距，再按反馈继续打磨。预览：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/
