# 当前进展

当前事项为 `design-astra-001`，状态以 `feature_list.json` 为准。原型作为产品文档，保留现有 React/Babel、模拟数据与 S2，不做生产构建迁移或真实服务接入。

## 当前成果

- 我的身份页：小字号页面标题，发布状态靠近姓名；BAP ID、操作与简介按单列组织，长简介完整换行。实现与视觉以原型为准。

- 编辑页：收紧当前身份信息，标识使用辅助字号；头像与字段分组，资料字段增加留白，短屏字段间距为 24px。仅改 CSS，创建与确认页样式、文案、业务流程不变。
- 页面标题：平衡断行，中文行距 1.3、无负字距，窄屏使用较小一级 S2 字号。
- 公开身份卡：收紧正反面间距及卡外复制区，两面共享自适应高度；新增“翻面／翻回正面”按钮，支持键盘，保留拖动。长资料摘要、完整资料及链上记录折叠继续可用。
- 页头滚动吸顶；320px 已连接页头高 120px。全局提示位于底部，6 秒自动消失，支持暂停与手动关闭。
- 原型场景与示例说明集中于右下角设置；授权弹窗明确外部钱包边界。

## 本次成果与验证

用户反馈公开身份页看不到翻面入口。宿主服务及实际浏览器确认按钮存在，原样式是无边框文字；用户浏览器是否加载旧缓存未证实。

- 翻面按钮采用既有 S2 secondary 样式，带 2px 边框；位于卡片右上方。
- 应用 CSS、文案与 JSX 引用添加资源版本；普通刷新后实际请求四个带 `v=20260917-flip` 的资源。
- 1440×1000 中文浅色正常资料、320×568 手机截图已复核；窄屏按钮为 76×44px，无横向溢出。
- 点击可翻到背面，Enter 可回正面，焦点保留。浏览器错误检查无输出，`git diff --check` 通过。未执行全量回归、真实钱包或用户浏览器缓存检查。
- 评审页同步当前按钮截图。

证据：[入口可见性验证](../../designs/own-word-prototype-s2-astra-001/evidence/flip-visible-20260917/verification.json)与同目录截图。卡片布局及第 6 项既有证据分别位于 `evidence/card-layout-flip-20260917/`、`evidence/identity-layout-20260917/`，不计入本轮通过结果。

## 下一步

用户视觉复核第 4、5、6 项。第 1–3 项建议未应用。整体事项保持 `in-progress`，原型资产保持 `needs-review`。

原型：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/

评审：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/review-20260917/
