# 当前交接

## 当前任务

胡先生要求基于当前版本规划 v0.2.0 高保真原型，已确认只处理文字内容，Markdown 编辑器参考 xLog，发布必须支持身份签名和真实链上发布。本轮交付计划，尚未进入搭建或实现。

计划：[v0.2.0 内容原型实施计划](v0.2.0-内容原型实施计划-20260917-204043.md)。`feature_list.json` 中 `design-content-v020` 为 `not-started`，规划状态为待评审；原有 `design-astra-001` 保持 `in-progress`，没有新增第二个进行中事项。

## 已确认与待确认

- 基线为原型仓库 `dev@8d924ae`。原型及根仓库上轮均已建立并推送 `dev`。
- Markdown 参考 xLog。已检查本地 `reference/xLog-dev`：CodeMirror、双栏与手机预览切换、同步滚动、工具栏、本地草稿和统一渲染。
- 用户已明确“只处理文字内容”，范围待确认项已关闭，采用现有 Markdown Post 模型。
- 签名与发布验收必须提供真实 TxID、正文读回及 AIP 签名归属 BAP ID 的证据。P0 先核实钱包能力、网络与服务端点，演练模拟不能代替通过。
- 现有核心认知的 Content 为 Markdown Post；本轮未修改事实来源。标题、正文和导入大小约束在版本 PRD 中形成，不借用 xLog 规则冒充 Ownword 事实。

## 环境与范围建议

- 从当前原型 dev 建立 `prototype/v0.2.0-content` worktree，目录建议为 `designs/own-word-prototype-v0.2.0/`，保留旧版并行预览。
- 沿用 React 18.3.1、Babel 和 S2；用 npm 锁文件与 esbuild 构建编辑器、渲染器和经验证的钱包适配依赖。已检查本机 Node 24.14.0、npm 11.18.0。
- 保留现有 4311 静态服务；新目录尚未创建。接入真实钱包、BAP 解析、广播及查询服务，优先使用现有可用端点；只有浏览器直连受限时才增加最小服务适配层。
- 顺序为 P0 范围、环境与最小真实签名发布链路验证，P1 写作与草稿，P2 真实发布与阅读 UI，P3 Revision 与交付。详情和验收以计划为准。
- 实施前建立实际 v0.2.0 PRD，登记版本来源并同步提交版本约定；计划不替代 PRD。

## 本轮验证与工作区

计划引用、JSON 结构和差异格式已核查，并核对钱包与 Post 协议文档。当前钱包的具体 AIP 签名能力、网络与费用、状态映射和 Revision 协议关联仍需 P0 验证，详见计划。未执行环境安装、连接钱包或发送交易，不把上轮测试当作 v0.2.0 通过。

当前服务：http://127.0.0.1:4311/own-word-prototype-s2-astra-001/ 。浏览器会话已在上轮关闭，服务保持运行。根仓库原有技能文件删除与新增保留，不纳入计划提交。

## 唯一下一步

启动 P0：形成版本 PRD、准备独立环境，核实真实钱包/BAP/AIP/Post 能力与服务端点，安排最小签名发布链路验证。
