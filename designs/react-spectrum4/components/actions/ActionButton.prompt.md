# ActionButton

ActionButton 复刻 Spectrum 2 图标动作按钮，用于动作原型。

```jsx
<ActionButton isSelected={false} aria-label="Add" />
```
仅图标、方形胶囊；selected 显示填充 accent。本组件是原型视觉复刻；生产可访问性与行为以 `@react-spectrum/s2` 为准。

